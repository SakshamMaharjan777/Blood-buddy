package com.bloodbuddy.model;

/**
 * Account status managed by administrators (report Table 3-2 userId row;
 * the admin dashboard's Active/Suspended toggle).
 */
public enum UserStatus {
    ACTIVE,
    SUSPENDED
}
